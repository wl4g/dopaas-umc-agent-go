#post uri
server-uri: http://10.0.0.26:14046/umc/basic

#post mode==kafka,http(default)
post-mode: kafka

#submit mode(default together) , delay together or separate
together-or-separate: together

#physical
physical:
  #delay(ms)
  delay: 10000
  #network card
  net: en0
  gather-port: 22,9092

#kafka
kafka:
  url: localhost:9092
  topic: __devops_umc_agent_metric_
  partiations: 10


